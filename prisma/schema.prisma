generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model StockData {
  id        Int     @id @default(autoincrement())
  sembol    String
  son       String
  farkYuzde String
  fark      String
  alis      String
  satis     String
  gYuksek   String
  gDusuk    String
  aOrt      String
  adet      String
  hacim     String
  süre     String?
}
